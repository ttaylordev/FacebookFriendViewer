```javascript


// var cors = require( 'cors' );
// var bodyParser = require( 'body-parser' );
// var mongoose = require( 'mongoose' );

// Controllers
// var meCtrl = require( './controllers/meCtrl' ); //does not need a .js, it knows that if you are requiring it, that it should be a js file.

// var mongoURI = 'mongodb://localhost:27017/DatabaseName';

//mongoDB connection
// mongoose.set( 'debug', true );
// mongoose.connect( mongoURI );
// mongoose.connection.once( 'open', function () {
//   console.log( 'Connected to mongo at: ', mongoURI );
// } )

//endpoints
// create end point
// app.post( '/me', meCtrl.create );

// update end point
// app.put( '/me/:id', meCtrl.update );

//delete end point
// app.delete( '/me/:id', meCtrl.delete );

// app.use( cors() );
// app.use( bodyParser.json() );
// app.use( express.static( __dirname + './../public' ) );
// NOTE: tells it to host this static page for us Local through Nodemon.


```
